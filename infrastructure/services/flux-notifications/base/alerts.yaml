apiVersion: notification.toolkit.fluxcd.io/v1beta1
kind: Alert
metadata:
  name: alerts
  namespace: flux-system
spec:
  summary: "local development cluster"
  providerRef:
    name: slack
  eventSeverity: info
  eventSources:
    - kind: GitRepository
      name: '*'
    - kind: Kustomization
      name: '*'
    - kind: HelmRelease
      name: '*'
